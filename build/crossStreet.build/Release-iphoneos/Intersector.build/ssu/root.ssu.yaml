apiVersion: ssu/v1
nlu:
  shortcuts:
    bundleIdentifier: com.marconius.crossStreet
    intents:
    - metadata:
        name: NearestIntersectionIntent_0
        title: Nearest Intersection
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }Nearest intersection in ${+applicationName}'
          - '{${+prefix} }Where is the nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s my nearest intersection with ${+applicationName}'
          - '{${+prefix} }What is my nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s the nearest intersection with ${+applicationName}'
          - '{${+prefix} }What intersection am I near with ${+applicationName}'
    - metadata:
        name: UpcomingIntersectionIntent_1
        title: Upcoming Intersection
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }Upcoming intersection in ${+applicationName}'
          - '{${+prefix} }What intersection is ahead with ${+applicationName}'
          - '{${+prefix} }What''s my upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What is my upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What''s the next intersection with ${+applicationName}'
          - '{${+prefix} }What intersection is coming up with ${+applicationName}'
    - metadata:
        name: SecondNearestIntersectionIntent_2
        title: 2nd Nearest Intersection
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }2nd nearest intersection in ${+applicationName}'
          - '{${+prefix} }Second nearest intersection in ${+applicationName}'
          - '{${+prefix} }What''s my 2nd nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s my second nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s the 2nd nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s the second nearest intersection with ${+applicationName}'
          - '{${+prefix} }What is my second nearest intersection with ${+applicationName}'
          - '{${+prefix} }Find my second nearest intersection with ${+applicationName}'
    - metadata:
        name: ThirdNearestIntersectionIntent_3
        title: 3rd Nearest Intersection
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }3rd nearest intersection in ${+applicationName}'
          - '{${+prefix} }Third nearest intersection in ${+applicationName}'
          - '{${+prefix} }What''s my 3rd nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s my third nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s the 3rd nearest intersection with ${+applicationName}'
          - '{${+prefix} }What''s the third nearest intersection with ${+applicationName}'
          - '{${+prefix} }What is my third nearest intersection with ${+applicationName}'
          - '{${+prefix} }Find my third nearest intersection with ${+applicationName}'
    - metadata:
        name: SecondUpcomingIntersectionIntent_4
        title: 2nd Upcoming Intersection
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }2nd upcoming intersection in ${+applicationName}'
          - '{${+prefix} }Second upcoming intersection in ${+applicationName}'
          - '{${+prefix} }What''s my 2nd upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What''s my second upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What''s the 2nd upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What''s the second upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What is my second upcoming intersection with ${+applicationName}'
          - '{${+prefix} }Find my second upcoming intersection with ${+applicationName}'
    - metadata:
        name: ThirdUpcomingIntersectionIntent_5
        title: 3rd Upcoming Intersection
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }3rd upcoming intersection in ${+applicationName}'
          - '{${+prefix} }Third upcoming intersection in ${+applicationName}'
          - '{${+prefix} }What''s my 3rd upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What''s my third upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What''s the 3rd upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What''s the third upcoming intersection with ${+applicationName}'
          - '{${+prefix} }What is my third upcoming intersection with ${+applicationName}'
          - '{${+prefix} }Find my third upcoming intersection with ${+applicationName}'
    - metadata:
        name: MyDirectionIntent_6
        title: My Direction
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }My direction in ${+applicationName}'
          - '{${+prefix} }Which way am I facing with ${+applicationName}'
          - '{${+prefix} }What direction am I facing with ${+applicationName}'
          - '{${+prefix} }What way am I facing with ${+applicationName}'
          - '{${+prefix} }Where am I facing with ${+applicationName}'
    - metadata:
        name: StartPointScanIntent_7
        title: Start Point and Scan
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }Start Point and Scan in ${+applicationName}'
          - '{${+prefix} }Scan for intersections with ${+applicationName}'
    - metadata:
        name: +negative
      corpuses:
        training:
        - locale: en
          utterances:
          - '{${+prefix} }${+standardAppShortcutNegatives}'
variables:
- name: +applicationName
  type: ssu/expansion
  definitions:
  - locale: en
    synomyms:
    - Intersector
- name: +prefix
  type: ssu/expansion
  definitions:
  - locale: en
    synomyms:
      $ref: ssu://Resources/prefix/en.yaml
- name: +standardAppShortcutNegatives
  type: ssu/expansion
  definitions:
  - locale: en
    synomyms:
      $ref: ssu://Resources/standardAppShortcutNegatives/en.yaml
